return {
  { "folke/snacks.nvim", opts = { terminal = { enabled = false } } },

  -- Optional: completely disable Snacks terminal if you really hate it
  -- { "folke/snacks.nvim", enabled = false },
}
